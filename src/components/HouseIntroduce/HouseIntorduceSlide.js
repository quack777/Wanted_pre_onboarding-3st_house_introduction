import React from "react";

const HouseIntorduceSlide = ({ imageUrl }) => {
    return (
        <div>
            <img src={imageUrl} />
        </div>
    );
};

export default HouseIntorduceSlide;
